# fly.toml app configuration file generated for fly-rust-axum-starter on 2024-05-10T12:15:25+02:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'framer-university-dev-backend'
primary_region = 'lhr'

[build]

[env]
PORT = "8080"
IP = "0.0.0.0"
ALLOWED_ORIGINS = ["http://localhost:3000", "http://127.0.0.1:3000"]
INSTANCE_METRICS_LOG_EVERY_SECONDS = 60
JWT_ACCESS_TOKEN_EXPIRATION_HOURS = 24
JWT_REFRESH_TOKEN_EXPIRATION_DAYS = 7
EMAIL_VERIFICATION_EXPIRATION_HOURS = 48
CONNECTION_TIMEOUT_SECONDS = 3
POOL_SIZE = 3

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
